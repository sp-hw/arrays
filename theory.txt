Массивы в Java представляют собой упорядоченный набор элементов одного типа данных. Каждый элемент в массиве имеет свой индекс, который начинается с нуля.

Массив хранится в памяти компьютера как единое целое.

Как устроен массив?

Как обойти массив? А в обратном порядке?

Скорость доступа к данным.
